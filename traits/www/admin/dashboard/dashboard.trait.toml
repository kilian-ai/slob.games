[trait]
description = "Admin dashboard — users, games, and system overview (admin-only)"
version = "v260410"
author = "system"
tags = ["www", "admin", "dashboard"]
provides = ["www/webpage"]

[signature]

[signature.returns]
type = "string"
description = "HTML admin dashboard page"

[implementation]
language = "rust"
source = "builtin"
entry = "dashboard"
wasm = true
